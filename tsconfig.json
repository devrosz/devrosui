// Main configuration to use path alias for importing components from the lib dir into
// the documentation site and storybook instead of clungy long relative paths.
{
    "compilerOptions": {
        "jsx": "react-jsx",
        "paths": {
            "@devrosui/react": ["./lib/src/index.ts"]
        }
    }
}